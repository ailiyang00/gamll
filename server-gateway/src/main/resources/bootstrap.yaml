spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      discovery:
      server-addr: 192.168.56.10:8848
      config:
        name: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace: DEFAULT


